import SectionLayout from './SectionLayout'

export default function AgentsContent() {
  return (
    // Wrap the page content in the shared section layout (consistent title/spacing).
    <SectionLayout titleId="agents-heading" title="Agents">
      {/* Short placeholder copy for the Agents page; replace with your real content. */}
      <p>Describe agents, automations, or AI-assisted workflows you work on.</p>
    </SectionLayout>
  )
}
